        VStack(spacing: 8) {
            if langStr != "ar" {
                Capsule()
                    .frame(width: labelWidth, height: 5)
            }
            
            
            Text(text)
                .font(Font(textFont as CTFont))
                .fixedSize()
                .onAppear {
                    calcDimintions()
                }
            
            if langStr == "ar" {
                Capsule()
                    .frame(width: labelWidth, height: 5)
            }
        }
        .frame(width: labelHeight + 13, height: labelWidth)
        .rotationEffect(Angle(degrees: textDirection == .up ? -90 : 90))
